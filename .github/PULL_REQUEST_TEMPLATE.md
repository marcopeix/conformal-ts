## Summary

What this PR changes, in one or two sentences.

## Related issue

Closes #...  (or remove this section if there's no issue)

## Checklist

- [ ] Tests added or updated
- [ ] Documentation updated (docstrings, README, CLAUDE.md if architecture changed)
- [ ] `uv run ruff check . && uv run ruff format --check . && uv run mypy src/ && uv run pytest` passes locally

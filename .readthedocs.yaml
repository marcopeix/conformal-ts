version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"
  jobs:
    post_create_environment:
      - pip install uv
    post_install:
      - uv sync --group docs --frozen

mkdocs:
  configuration: mkdocs.yml
  fail_on_warning: false

formats:
  - htmlzip
